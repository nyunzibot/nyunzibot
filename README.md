# nyunzi_bot (refactored)

This is your original `bot.py` split into multiple files for easier editing.

## Run
From this folder:

```bash
python main.py
```

(Same dependencies as before: discord.py v2+, aiohttp, pillow)

## Notes
- Logic/behavior is intentionally preserved from the original single-file bot.
